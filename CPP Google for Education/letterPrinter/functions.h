#include<iostream>
#include<iomanip>
#include<vector>

std::vector<std::vector<int> > selector(char letter);
void plotter(char letter, std::vector<std::vector<int> > coordinates);
std::string getString();